<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/scrollVacaciones"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- El resto de tus vistas van aquí dentro sin repetir los xmlns -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- CABECERA: TÍTULO Y FILTRO DE AÑO -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="24dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="GESTIÓN DE AUSENCIAS"
                    android:textColor="#059669"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.15" />
                <TextView
                    android:id="@+id/tvTituloVacaciones"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Mis Vacaciones"
                    android:textColor="@color/text_main"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:layout_marginTop="4dp" />
            </LinearLayout>

            <Spinner
                android:id="@+id/spAnioVacaciones"
                android:layout_width="125dp"
                android:layout_height="48dp"
                android:layout_marginStart="10dp"
                android:background="@android:drawable/btn_dropdown"
                android:spinnerMode="dropdown" />
        </LinearLayout>

        <!-- TARJETA CONTENEDORA PARA EL LISTADO DE PERIODOS -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="PERIODOS SOLICITADOS ESTE AÑO"
            android:textColor="@color/text_main"
            android:textSize="11sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp"
            android:letterSpacing="0.05" />

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="320dp"
            android:layout_marginBottom="24dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/bg_app">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvVacaciones"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="8dp" />
        </androidx.cardview.widget.CardView>

        <!-- DIVISOR ESTILIZADO -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/bg_fecha_disabled"
            android:layout_marginBottom="20dp" />

        <!-- SECCIÓN NUEVO PERIODO (ZONA DE ENTRADA) -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SOLICITAR NUEVO PERIODO"
            android:textColor="@color/text_main"
            android:textSize="11sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            android:letterSpacing="0.05" />

        <!-- Campos de Selección de Fecha (Lado a Lado) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="20dp">

            <!-- Fecha Inicial -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardFechaInicioContenedor"
                android:layout_width="0dp"
                android:layout_height="64dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                app:cardCornerRadius="14dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@color/bg_fecha_disabled">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:background="?android:attr/selectableItemBackground">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Fecha Inicio"
                        android:textColor="#059669"
                        android:textSize="11sp"
                        android:textStyle="bold" />
                    <TextView
                        android:id="@+id/tvFechaInicio"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="-- / -- / ----"
                        android:textColor="@color/text_disabled"
                        android:textSize="14sp"
                        android:layout_marginTop="4dp" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>

            <!-- Fecha Final -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardFechaFinContenedor"
                android:layout_width="0dp"
                android:layout_height="64dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                app:cardCornerRadius="14dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@color/bg_fecha_disabled">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:background="?android:attr/selectableItemBackground">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Fecha Fin"
                        android:textColor="#059669"
                        android:textSize="11sp"
                        android:textStyle="bold" />
                    <TextView
                        android:id="@+id/tvFechaFin"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="-- / -- / ----"
                        android:textColor="@color/text_disabled"
                        android:textSize="14sp"
                        android:layout_marginTop="4dp" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <!-- Indicador de total de días calculados de forma dinámica -->
        <TextView
            android:id="@+id/tvTotalDiasCalculados"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Días seleccionados: 0 días"
            android:textColor="@color/text_main"
            android:textSize="13sp"
            android:gravity="center"
            android:layout_marginBottom="24dp"
            android:visibility="gone" />

        <!-- BOTÓN DE CONFIRMACIÓN -->
        <Button
            android:id="@+id/btnGuardarVacaciones"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Registrar Periodo"
            android:backgroundTint="@color/bg_btn_disabled"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="16sp"
            android:enabled="false"
            app:cornerRadius="14dp" />

    </LinearLayout>
</ScrollView>